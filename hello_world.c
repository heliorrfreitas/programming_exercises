#include <stdio.h>

/*
To compile use the following command:

cc -std=c99 -Wall hello_world.c -o hello_world

cc is the compiler

-std= lets the standar be specified

c99 is the standard used to compile the c code

-Wall it displays almost all warning of gcc

-o means to place the output into a file

*/

int main(){
	
	printf("Hello world!\n");
	
	return 0;
}
